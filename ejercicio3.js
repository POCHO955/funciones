//3. Calcular el área de un triángulo

let areaTriangulo = (base, altura) => (base * altura) / 2;

console.log(areaTriangulo(25, 5)); 

let triangulo = (base, altura) => {
    let resultado = (base * altura);

    return resultado;
}

console.log(triangulo(10,20));