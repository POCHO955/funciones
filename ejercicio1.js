    //1. Calcular el cuadrado de un número 

    // const cuadrado = num => num * num;

    // console.log(cuadrado(16));
    
    let cuadrado = (num) => {
        let resultado = num * num;
        
        return resultado;
    }

    console.log(cuadrado(34));